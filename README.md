# Dotfiles

This repository contains my personal configuration files (dotfiles) for both macOS and Linux systems.

## Installation

### macOS

```bash
# Copy all configs to ~/.config
cp -r nvim bat sketchybar ghostty ~/.config/ 2>/dev/null && cp starship.toml ~/.config/ 2>/dev/null && cp .zshrc ~/ 2>/dev/null
```